<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vbfc_bry07.calls">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.HomeActivity" />
        <activity android:name=".Activity.ACPActivity" />
        <activity android:name=".Activity.CycleCalendarActivity" />
        <activity android:name=".Activity.SignatureFormActivity" />
        <activity
            android:name=".Activity.MCPActivity"
            android:icon="@mipmap/ic_launcher"
            android:label="Master Coverage Plan" />
        <activity android:name=".Activity.CalendarActivity" />
        <activity android:name=".Activity.ListViewActivity" />
    </application>

</manifest>
